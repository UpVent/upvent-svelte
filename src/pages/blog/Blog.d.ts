// For Blog.svelte
export type Posts = {
    _embedded: any,
    slug: string,
    title: {
        rendered: string
    },
    excerpt: {
        rendered: string
    }
};


// For Post.svelte
export type Post = {
    
};